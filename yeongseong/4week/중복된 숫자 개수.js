function solution(array, n) {
    var answer = 0;
    for (var i = 0; i < array.length; i++){ 
        if (n === array[i]){
            answer++; // n과 배열 요소가 일치하는 경우 answer를 1 증가
        }else{            
        }
    }
    return answer;
}